Task: Test HTML validity and semantics
Worker: worker_html_validator_T4
Specialty: qa_html_validator
Team: qa
Generated: 1755567162.668181
--------------------------------------------------
Findings for dashboard.html:

[FAIL] ARIA and Accessibility
- Missing aria-labels for interactive elements
- Progress bars lack aria-valuenow/valuemin/valuemax attributes
- Button uses icon without accessible label or alt text

[WARN] Color Contrast
- Some status colors may not meet WCAG 2.1 AA contrast requirements
- Link color in project objective needs contrast verification

[FAIL] Semantic Structure
- Using div.header instead of semantic <header>
- Missing main landmark
- Navigation regions not marked with <nav>

Recommended fixes:
1. Add ARIA attributes to progress bar:
```html
<div class="progress-bar" role="progressbar" aria-valuenow="{{ progress }}" aria-valuemin="0" aria-valuemax="100">
```

2. Replace div.header with semantic <header>:
```html
<header class="header">
```

3. Add descriptive aria-label to refresh button:
```html
<button class="refresh-btn" aria-label="Refresh dashboard" onclick="location.reload()">
```

4. Add main landmark:
```html
<main class="container">
```